This directory contains various infra/config files needed for SDS testing.

demo-server.tmpl

----------Older files-------------------
grpc-sds-service.yaml (for creating server deployment to use ClusterIP so
    client can use grpc-sds-service as the server hostname)
